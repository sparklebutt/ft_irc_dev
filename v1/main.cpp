bool validate_port();
bool validate_password();


int main(int argc, char** argv)
{
	int port_number = 4242;
	std::string password = "password"

	if(!)
	{
		port=4242
		password=password
	}

	if(argv == 2)
	{
		validate input - validate_port(argv[1]), is(password)
		port = argv[1];
		server_class(password, port_number)
	}

	server(port, password)
	open_socket()
	loop

	return 0;
}